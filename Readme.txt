Deploy the solidity code with injected metamask provider network.
Run the front end HTML code using Visusal Studio Code Live server.
To add or prioritize a task enter both task name and priority.
To delete a task enter the task id which is the task position in the list (from 0 to size-1)
